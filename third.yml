name: third workflow
on: workflow_dispatch
jobs:
 first-job:
   runs-on: ubuntu-latest
   steps:
     - name: hello github action
       run: echo "hello this is my first "
     - name: hello github action
       run: echo "hello this is my second"
